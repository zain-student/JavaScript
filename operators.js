// Logical & operator
var score = 8;
console.log("Mid Level Skill:", score > 0 && score < 10);

// Logical OR || operator
var timeRemaining = 0;
var energy = 10;
console.log("Game Over:", timeRemaining == 0 || energy == 0);
